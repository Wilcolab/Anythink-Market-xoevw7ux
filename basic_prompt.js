/**
 * Write a function called `toCamelCase` that takes a string as input and returns the string converted to camelCase.
 * The function should handle strings with spaces, dashes, or underscores as word separators.
 *
 * Example:
 * toCamelCase("hello world") // returns "helloWorld"
 * toCamelCase("convert-this_string") // returns "convertThisString"
 */